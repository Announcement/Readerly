let block = 'block'
let inline = 'inline'
let runIn = 'run-in'

let flow = 'flow'
let flowRoot = 'flow-root'
let table = 'table'
let flex = 'flex'
let grid = 'grid'
let ruby = 'ruby'
let subgrid = 'subgrid'

let inlineBlock = 'inline-block'
let inlineTable = 'inline-table'
let inlineFlex = 'inline-flex'
let inlineGrid = 'inline-grid'

// missing some silly stuff.

export {
  block,
  inline,
  runIn,
  flow,
  flowRoot,
  table,
  flex,
  grid,
  ruby,
  subgrid,
  inlineBlock,
  inlineTable,
  inlineFlex,
  inlineGrid
}
